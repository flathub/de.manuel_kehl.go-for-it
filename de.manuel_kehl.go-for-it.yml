id: de.manuel_kehl.go-for-it
runtime: org.gnome.Platform
runtime-version: 3.30
sdk: org.gnome.Sdk
command: de.manuel_kehl.go-for-it
finish-args:
  # X11 + XShm access
  - --share=ipc
  - --socket=x11
  - --socket=fallback-x11
  # Wayland access
  - --socket=wayland
  # Access user files
  - --filesystem=home
  # Dconf access
  - --filesystem=xdg-run/dconf
  - --filesystem=~/.config/dconf:ro
  - --talk-name=ca.desrt.dconf
  - --env=DCONF_USER_CONFIG_DIR=.config/dconf
  # Notification access
  - --talk-name=org.freedesktop.Notifications
cleanup:
  - '*.a'
  - '*.h'
  - '*.la'
  - /include
  - /lib/pkgconfig
modules:
  - name: go-for-it
    buildsystem: cmake-ninja
    builddir: true
    sources:
      - type: git
        url: 'https://github.com/JMoerman/Go-For-It.git'
        tag: 1.6.8
        commit: 09dee310138864efab0df6cf7a0103241f43d848
        # Temporary workaround until id can be switched to:
        # com.github.jmoerman.go-for-it
      - type: patch
        path: patch/go-for-it/0001-Temporary-app-id-workaround.patch
    post-install:
      - install -Dm644 -t /app/share/licenses/de.manuel_kehl.go-for-it ../COPYING
